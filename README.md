qTip2 Bower Package For Ember CLI
=====
This is just a wrapper that works with Ember CLI.


Clone into an existing `qtip2` repo clone, and run the ./bin/build script to generate the files. Then push to the repo.
